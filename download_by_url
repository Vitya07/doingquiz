#Этот код скачвает картинку с определенным url в эту папку

import urllib.request


url = input("Введите url картинки: ")
img = urllib.request.urlopen(url).read()
name_img = input("Введите название картики: (в конце названия указать то расширение, которое стоит в конце ссылки): ")
print("Картинка сохраняется в эту же папку) ")
out = open(name_img, "wb")
out.write(img)
input("Для закртыия нажмите любую клавишу ")
